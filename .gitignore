# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/

# data (regenerated at runtime if needed)
data/

# DO NOT ignore model/ — pkl files must be committed so Render can load them
# model/
